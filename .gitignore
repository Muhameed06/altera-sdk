# Dart / Flutter
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock

# IDE / OS
.idea/
*.iml
.vscode/
**/.DS_Store

# ALTERA — generated per-app config holds your API key; never commit it.
**/altera_config.dart
# Backups written by `dart run live_ui_bridge:wrap`.
*.bak
