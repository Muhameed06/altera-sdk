name: live_ui_bridge
description: ALTERA SDK — wrap a Flutter app once and edit its live UI (text, layout, order, A/B tests) from a dashboard over WebSockets.
version: 0.1.0
publish_to: none
homepage: https://github.com/Muhameed06/altera-sdk
repository: https://github.com/Muhameed06/altera-sdk

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
